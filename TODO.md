# Upgrade QR Studio — TODO

## Plan steps
- [x] Update `script.js` for request-staleness prevention, caching QR URLs, and safer image load handling.

- [x] Improve history performance using event delegation (avoid re-binding every render).

- [x] Add clipboard fallback when `navigator.clipboard` isn’t available.

- [x] Minor UX/accessibility polish in `style.css` (focus-visible, reduced layout shift).

- [x] Quick manual verification (generate, resize, theme toggle, history click, download, copy).


- [ ] Commit + push to GitHub.


